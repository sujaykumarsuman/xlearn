import { Placeholder } from "../components/Placeholder";

export default function Settings() {
  return (
    <Placeholder
      eyebrow="Account"
      title="Settings"
      icon="settings"
      sprint="S10"
      summary="Profile, study budget and timezone, reminders, and the API keys that power the AI coach."
    />
  );
}
