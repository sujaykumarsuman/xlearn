import { Placeholder } from "../components/Placeholder";

export default function Progress() {
  return (
    <Placeholder
      eyebrow="Analytics"
      title="Progress"
      icon="chart"
      sprint="S09"
      summary="Coverage, the revision heatmap, pattern mastery, rubric trend and outcome mix — read-model projections built from events."
    />
  );
}
