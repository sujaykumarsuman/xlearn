// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: account.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAccount = `-- name: CreateAccount :one
INSERT INTO identity.account (display_name, email)
VALUES ($1, $2)
RETURNING id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash
`

type CreateAccountParams struct {
	DisplayName string
	Email       pgtype.Text
}

func (q *Queries) CreateAccount(ctx context.Context, arg CreateAccountParams) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, createAccount, arg.DisplayName, arg.Email)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const createEmailAccount = `-- name: CreateEmailAccount :one
INSERT INTO identity.account (display_name, email, password_hash)
VALUES ($1, $2, $3)
RETURNING id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash
`

type CreateEmailAccountParams struct {
	DisplayName  string
	Email        pgtype.Text
	PasswordHash pgtype.Text
}

// Create an account from an email sign-up (ADR-0023): email is required + case-insensitively
// unique (partial index), and password_hash is the pre-computed bcrypt hash.
func (q *Queries) CreateEmailAccount(ctx context.Context, arg CreateEmailAccountParams) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, createEmailAccount, arg.DisplayName, arg.Email, arg.PasswordHash)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const getAccount = `-- name: GetAccount :one
SELECT id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash FROM identity.account
WHERE id = $1
`

func (q *Queries) GetAccount(ctx context.Context, id pgtype.UUID) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, getAccount, id)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const getAccountByEmail = `-- name: GetAccountByEmail :one
SELECT id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash FROM identity.account
WHERE lower(email) = lower($1)
`

// Look up an account by email, case-insensitively (email sign-in + link-by-email). Returns
// the row incl. password_hash — never serialised to a client.
func (q *Queries) GetAccountByEmail(ctx context.Context, lower string) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, getAccountByEmail, lower)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const getAccountByProviderIdentity = `-- name: GetAccountByProviderIdentity :one
SELECT a.id, a.display_name, a.email, a.timezone, a.study_budget_json, a.reminders_json, a.created_at, a.password_hash
FROM identity.account a
JOIN identity.oauth_identity oi ON oi.account_id = a.id
WHERE oi.provider = $1 AND oi.provider_user_id = $2
`

type GetAccountByProviderIdentityParams struct {
	Provider       string
	ProviderUserID string
}

func (q *Queries) GetAccountByProviderIdentity(ctx context.Context, arg GetAccountByProviderIdentityParams) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, getAccountByProviderIdentity, arg.Provider, arg.ProviderUserID)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const setAccountPassword = `-- name: SetAccountPassword :one
UPDATE identity.account
SET password_hash = $2
WHERE id = $1
RETURNING id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash
`

type SetAccountPasswordParams struct {
	ID           pgtype.UUID
	PasswordHash pgtype.Text
}

// Set or replace the account's bcrypt password hash (Settings: set/change password).
func (q *Queries) SetAccountPassword(ctx context.Context, arg SetAccountPasswordParams) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, setAccountPassword, arg.ID, arg.PasswordHash)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}

const updateAccount = `-- name: UpdateAccount :one
UPDATE identity.account
SET display_name      = COALESCE($2, display_name),
    timezone          = COALESCE($3, timezone),
    study_budget_json = COALESCE($4::jsonb, study_budget_json),
    reminders_json    = COALESCE($5::jsonb, reminders_json)
WHERE id = $1
RETURNING id, display_name, email, timezone, study_budget_json, reminders_json, created_at, password_hash
`

type UpdateAccountParams struct {
	ID              pgtype.UUID
	DisplayName     pgtype.Text
	Timezone        pgtype.Text
	StudyBudgetJson []byte
	RemindersJson   []byte
}

// Partial update of the caller's own account (PATCH /me, S10). A NULL narg leaves
// the column unchanged (COALESCE), so profile / budget / timezone / reminders can be
// saved independently. The jsonb blobs are validated + canonicalised in the handler.
func (q *Queries) UpdateAccount(ctx context.Context, arg UpdateAccountParams) (IdentityAccount, error) {
	row := q.db.QueryRow(ctx, updateAccount,
		arg.ID,
		arg.DisplayName,
		arg.Timezone,
		arg.StudyBudgetJson,
		arg.RemindersJson,
	)
	var i IdentityAccount
	err := row.Scan(
		&i.ID,
		&i.DisplayName,
		&i.Email,
		&i.Timezone,
		&i.StudyBudgetJson,
		&i.RemindersJson,
		&i.CreatedAt,
		&i.PasswordHash,
	)
	return i, err
}
