// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: stage_event.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createStageEvent = `-- name: CreateStageEvent :exec
INSERT INTO practice.stage_event (attempt_id, stage, unlocked_from)
VALUES ($1, $2, $3)
`

type CreateStageEventParams struct {
	AttemptID    pgtype.UUID
	Stage        string
	UnlockedFrom pgtype.Text
}

func (q *Queries) CreateStageEvent(ctx context.Context, arg CreateStageEventParams) error {
	_, err := q.db.Exec(ctx, createStageEvent, arg.AttemptID, arg.Stage, arg.UnlockedFrom)
	return err
}

const listStageEvents = `-- name: ListStageEvents :many
SELECT id, attempt_id, stage, unlocked_from, entered_at FROM practice.stage_event
WHERE attempt_id = $1
ORDER BY entered_at
`

// The stages entered for an attempt; the distinct content stages here are the
// authoritative "unlocked stages" for the problem (R-PF1).
func (q *Queries) ListStageEvents(ctx context.Context, attemptID pgtype.UUID) ([]PracticeStageEvent, error) {
	rows, err := q.db.Query(ctx, listStageEvents, attemptID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PracticeStageEvent{}
	for rows.Next() {
		var i PracticeStageEvent
		if err := rows.Scan(
			&i.ID,
			&i.AttemptID,
			&i.Stage,
			&i.UnlockedFrom,
			&i.EnteredAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
