// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: user_problem_state.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getUserProblemState = `-- name: GetUserProblemState :one
SELECT id, account_id, problem_id, status, first_solved_at, current_touch, last_outcome, created_at, updated_at FROM practice.user_problem_state
WHERE account_id = $1 AND problem_id = $2
`

type GetUserProblemStateParams struct {
	AccountID pgtype.UUID
	ProblemID string
}

func (q *Queries) GetUserProblemState(ctx context.Context, arg GetUserProblemStateParams) (PracticeUserProblemState, error) {
	row := q.db.QueryRow(ctx, getUserProblemState, arg.AccountID, arg.ProblemID)
	var i PracticeUserProblemState
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.ProblemID,
		&i.Status,
		&i.FirstSolvedAt,
		&i.CurrentTouch,
		&i.LastOutcome,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listUserProblemStates = `-- name: ListUserProblemStates :many
SELECT id, account_id, problem_id, status, first_solved_at, current_touch, last_outcome, created_at, updated_at FROM practice.user_problem_state
WHERE account_id = $1
  AND problem_id = ANY($2::text[])
`

type ListUserProblemStatesParams struct {
	AccountID  pgtype.UUID
	ProblemIds []string
}

func (q *Queries) ListUserProblemStates(ctx context.Context, arg ListUserProblemStatesParams) ([]PracticeUserProblemState, error) {
	rows, err := q.db.Query(ctx, listUserProblemStates, arg.AccountID, arg.ProblemIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PracticeUserProblemState{}
	for rows.Next() {
		var i PracticeUserProblemState
		if err := rows.Scan(
			&i.ID,
			&i.AccountID,
			&i.ProblemID,
			&i.Status,
			&i.FirstSolvedAt,
			&i.CurrentTouch,
			&i.LastOutcome,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setStateAttempting = `-- name: SetStateAttempting :one
UPDATE practice.user_problem_state
SET status = 'attempting', updated_at = now()
WHERE account_id = $1 AND problem_id = $2
RETURNING id, account_id, problem_id, status, first_solved_at, current_touch, last_outcome, created_at, updated_at
`

type SetStateAttemptingParams struct {
	AccountID pgtype.UUID
	ProblemID string
}

func (q *Queries) SetStateAttempting(ctx context.Context, arg SetStateAttemptingParams) (PracticeUserProblemState, error) {
	row := q.db.QueryRow(ctx, setStateAttempting, arg.AccountID, arg.ProblemID)
	var i PracticeUserProblemState
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.ProblemID,
		&i.Status,
		&i.FirstSolvedAt,
		&i.CurrentTouch,
		&i.LastOutcome,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const setStateSolved = `-- name: SetStateSolved :one
UPDATE practice.user_problem_state
SET status = 'solved',
    last_outcome = $3,
    first_solved_at = COALESCE(first_solved_at, now()),
    updated_at = now()
WHERE account_id = $1 AND problem_id = $2
RETURNING id, account_id, problem_id, status, first_solved_at, current_touch, last_outcome, created_at, updated_at
`

type SetStateSolvedParams struct {
	AccountID   pgtype.UUID
	ProblemID   string
	LastOutcome pgtype.Text
}

// Mark solved, latching first_solved_at on the first solve and recording the last
// outcome. current_touch stays as-is (review owns the five-touch progression).
func (q *Queries) SetStateSolved(ctx context.Context, arg SetStateSolvedParams) (PracticeUserProblemState, error) {
	row := q.db.QueryRow(ctx, setStateSolved, arg.AccountID, arg.ProblemID, arg.LastOutcome)
	var i PracticeUserProblemState
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.ProblemID,
		&i.Status,
		&i.FirstSolvedAt,
		&i.CurrentTouch,
		&i.LastOutcome,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertUserProblemState = `-- name: UpsertUserProblemState :one
INSERT INTO practice.user_problem_state (account_id, problem_id, status)
VALUES ($1, $2, 'available')
ON CONFLICT (account_id, problem_id)
DO UPDATE SET updated_at = now()
RETURNING id, account_id, problem_id, status, first_solved_at, current_touch, last_outcome, created_at, updated_at
`

type UpsertUserProblemStateParams struct {
	AccountID pgtype.UUID
	ProblemID string
}

// Get-or-create the (account, problem) state row, touching updated_at so the row is
// always returned (ON CONFLICT DO NOTHING would return nothing on the resume path).
func (q *Queries) UpsertUserProblemState(ctx context.Context, arg UpsertUserProblemStateParams) (PracticeUserProblemState, error) {
	row := q.db.QueryRow(ctx, upsertUserProblemState, arg.AccountID, arg.ProblemID)
	var i PracticeUserProblemState
	err := row.Scan(
		&i.ID,
		&i.AccountID,
		&i.ProblemID,
		&i.Status,
		&i.FirstSolvedAt,
		&i.CurrentTouch,
		&i.LastOutcome,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
