# Prompt — Sprint m3-12 · Results dock, Problems, Arena, degradation badges (AB08–AB11)

> **One self-contained prompt = one sprint = one session.** Paste into a fresh coding session at the repo root.
> **Plan:** [`../sprints/sprint-m3-12.md`](../sprints/sprint-m3-12.md)   ·   **Milestone:** M3 (M3-2 Run/Submit, UI slice 2 of 3)   ·   **Prereqs:** [m3-11](../sprints/sprint-m3-11.md), [ds-m3-02](../sprints/sprint-ds-m3-02.md) (+ [ds-m3-01](../sprints/sprint-ds-m3-01.md), [m3-09](../sprints/sprint-m3-09.md), [m3-14](../sprints/sprint-m3-14.md))

## Read first

- [`../../../CLAUDE.md`](../../../CLAUDE.md) / [`../../../AGENT.md`](../../../AGENT.md) — conventions, land-and-sync.
- The plan: [`../sprints/sprint-m3-12.md`](../sprints/sprint-m3-12.md) (the state/copy table and the compose-reachability table are there).
- **The frozen boards** (authoritative for layout and copy): `design-system/screens/v2/AB08-results-dock.html`, `design-system/screens/v2/AB09-problems.html`, `design-system/screens/v2/AB10-arena.html`, `design-system/screens/v2/AB11-degradation-badges.html`; v1 reference [`Problem.dc.html`](../../../design-system/screens/Problem.dc.html) (dock at `:289-302`). Tokens and components: [`design-system/README.md`](../../../design-system/README.md), `design-system/theme.css`.
- [t4 §8](../research/t4-judge-contract.md#8-learner-flows-and-v2-artboards-needed) (results-dock copy, arena frames), **t4 §2.6** (learner DTO + denylist), **§2.7** (polling, settling), **§3.7** (arena mechanics) and **§13** (D17 overrides §3.7/§8's arena locks and reveal cap) in [`../research/t4-judge-contract.md`](../research/t4-judge-contract.md).
- [ADR-0029](../../adr/0029-judge-contract-and-learning-signal.md) §2 (contract, DTO allowlist), §4 (arena: unrestricted, own done marker); [PRD R-AR1 (amended)](../../prd/xlearn-v2-prd.md); [ADR-0034 §2](../../adr/0034-v2-release-labelling-gating-and-rollback.md#2-feature-gating-three-tiers-no-flag-service) (presence by config, T-3 cohort, lazy chunks); [ADR-0035](../../adr/0035-v2-operations-nats-auth-limits-capacity.md) §4 (L6 typed 413, L9–L11 quotas).
- [rollout §9](../rollout-plan.md#9-artboards-by-milestone) (AB08–AB11, freeze rule), [§5](../rollout-plan.md#5-m3-hard-entry-checklist) (M3 checklist).
- Code: `web/src/screens/{Workspace,Problems,Problem}.tsx`, `web/src/lib/{judge,api,curriculum,progress}.ts`, `web/src/router.tsx`, `web/src/components/{States,Icon}.tsx`, `web/src/styles/app.css`; the BFF routes m3-09 added (`apiRoutes()` in `internal/gateway/bff.go`, `docs/architecture/openapi.yaml`); the fixture sources `internal/judge/testdata/{content,packsrc}` (the built `pack/` is regenerated by `internal/packspec/fixture_test.go -update`), m3-06's synthetic learner solutions `internal/judge/testdata/solutions/<item>/`, m3-06's `internal/e2e/judge_runner_test.go` and the `judge-runner-e2e` job in `.github/workflows/ci.yml`; `internal/e2e/`; `deploy/local/` (compose, the `runner` profile, the dev content overlay).

## Context

M3-1 (`v1.13.0`, [m3-07](../sprints/sprint-m3-07.md)) put judge on production dark. M3-2 ships in **`v1.14.0`**: practice's judge consumer (m3-08), the gateway BFF (m3-09), review/assessment on judge signals (m3-10), the Workspace (m3-11), **this sprint**, and m3-13, which adds AB12, runs the M3 exit tests, tags and flips `JUDGE_BASE_URL` for the owner and tester cohort. This sprint is web code (plus an e2e test and synthetic fixture data): the results dock inside m3-11's Workspace, Problems with course and arena markers (D10), the Arena (unrestricted per D17; never counts per D10) and the degradation badges that replace alerting in the product (D34). Everything is behind m3-09's presence-by-config gate, so it ships dark. v2 is owner-only use (D35).

## Entry gates — verify first (stop and report if any is unmet)

- [ ] AB07–AB12 frozen: the ds-m3-01 and ds-m3-02 PRs are merged with the owner's approval (`git log origin/main -- design-system/screens/v2/`)
- [ ] [m3-11](../sprints/sprint-m3-11.md) merged: `Workspace.tsx` with its empty dock slot, lazy CodeMirror + `vite:preloadError` reload, `web/src/lib/judge.ts`
- [ ] [m3-09](../sprints/sprint-m3-09.md) merged: submissions/Runs polling with ETag, drafts, arena history/diff, arena progress read + `…/arena/studied`, arena reveal, `GET /api/judge/status`, typed 413/429 with quota fields, presence by config
- [ ] [m3-14](../sprints/sprint-m3-14.md) on `main`: learner DTO allowlist, arena history, `arena_progress` (`source auto|manual`)
- [ ] The M3 hard entry checklist was recorded green at m3-11's start and nothing regressed since
- [ ] No open peer PR edits `web/src/screens/{Workspace,Problems}.tsx`, `web/src/lib/judge.ts` or `web/src/router.tsx` (`gh pr list`, `git worktree list`, ListAgents)

## Do this (in order)

1. **[X] Branch** `feat/m3-results-arena` from an up-to-date `main`.
2. **[X] Results dock (AB08)** — `web/src/components/judge/ResultsDock.tsx`, mounted in m3-11's dock slot (the Results tab below 1024 px). Tabs **Samples · Submission · History · Feedback**. Use the BFF's **camelCase** field names (m3-09's `judge_dto.go` / `openapi.yaml`; the TS types mirror `openapi.yaml`, not t4's snake_case). Poll through `lib/judge.ts`: `pollAfterMs` 500 ms → 1 s → 2 s → 5 s with `If-None-Match`; stop at 5 min ("still grading, we'll show it on Today"); for counted contexts keep polling until `factsThroughSeq ≥ attemptSeq` (the **settling** state, ≤ 60 s after terminal). Implement every state in the plan's table with AB08's copy (F2–F15): queued/running/settling, accepted, WA (`37/40 hidden passed`), perf TLE, CE on own code and CE hidden API, RE/MLE, `inconclusive` / released close with **[Submit again]** (new Idempotency-Key), `contract_changed` (+ [Reload problem]), `not_evaluated_here`, 413 (64 KiB), the quota/429 family (incl. L12 `budget_exhausted` and the L13 breaker's `too_fast{reason: breaker}` / `shed{reason: breaker}`, whose copy says submits still work) with `Retry-After`, and the 503 `evaluation_unavailable{fallback: self}` line. Samples show Run detail (`sampleExpected` allowed) and learner-file diagnostics only; Submission shows `checks`, `hidden{passed,total}`, `perf`, `firstFailure.class`, `trust`, `evaluatedWith` and nothing else (TS types admit only allowlisted fields); History lists evaluation-bearing submissions (never Runs); Feedback shows post-conclusion key marks / `answerReveal` if present, else AB08's empty state. `aria-live="polite"` on the status line.
3. **[X] Problems (AB09)** — `web/src/screens/Problems.tsx`, course-resolved (`useCourse()`): course-done marker (practice state + grade chip) and arena-done marker (`firstPassedAt` from `GET /api/paths/{slug}/arena/progress`; `source=manual` = "Studied") from **one batched** read; filters exactly as AB09 F2 (status segment All · Not started · Course done · Arena done · Review due; difficulty; grading chips Judge-graded / Self-report; week; search by title or #) in the URL query. **No pattern filter:** it would reveal the pattern of live items (ADR-0027 §1 withhold leak). Show the pattern chip only as the BFF returns it (`withhold()`). Row actions for the cohort: Start/Resume (course-current packed items → Workspace) and Arena (every item; ahead-of-schedule items → arena only). **On a 404 from the arena route, render exactly the v1 list** (accounts outside the owner/tester cohort). AB09 copy replaces "Ahead · free practice".
4. **[X] Arena (AB10)** — `web/src/screens/Arena.tsx` on the Workspace shell, route `/:course/arena/:id` in `router.tsx`. Banner "Nothing here counts toward your course."; **no lock state at all** (D17 overrides t4's "Finish your review first", disabled Submit and withheld Solution/History); manual timer popover off by default, client-only (`sessionStorage`, try/catch), no events; Submit/Run with `context: arena` into the same dock; **History drawer** (arena submits only, read-only) with **Copy to editor** (confirm when dirty) and **Diff** via `@codemirror/merge` inside the lazy editor chunk; **spoiler confirm** on Show solution → m3-09's reveal route, copy says it is recorded and **doesn't cap** the course grade; **study mode** for items with no evaluator for this account (unpacked, or B/C) with **Mark studied** → `POST /api/problems/{id}/arena/studied`. AB09 F7 / AB10 F9 (judge off → "Studied" marker, Mark studied) can't be built because those routes 404 without judge. The v1 fallback wins: record it in the decisions log and flag it to the owner in the PR.
5. **[X] Badges (AB11)** — `web/src/components/judge/JudgeBadges.tsx` + `useJudgeStatus()` (`GET /api/judge/status` → `{state: ok|saturated|breaker|off, breakerLevel?, evaluable, items?}`, staleTime 30 s, refetch on focus; 404 → none). Follow the plan's table and AB11 F1–F6:
   - "Grading pending · self-report": `evaluable = 0` (page banner), or the item is `contract_mismatch` / `unsupported` / `no_pack` (row chip);
   - "Grader busy": `saturated`, breaker level 1 or 2, or a `queue_full` 429. Submits still work, and **nothing switches to self**;
   - "Auto-grading off": `state=off` (L15 `JUDGE_ADMISSION=closed`, or judge unreachable);
   - "xLearn AI paused": typed as an M4 placeholder, never shown in M3.

   **The Workspace follows practice's state, never the badge.** After a 503 on submit, the dock shows AB08 F15's line. The capped AB07 F11 picker appears only when practice reports `self_grade_pending`. An attempt that started judged never gets AB07 F12's uncapped picker. Placement: the Problems header (+ per-row chip), the Workspace HUD/cover (+ "Grader busy" in the dock header) and the course Dashboard, per AB11 F2–F4.
6. **[X] Tests** — vitest per AB08 state from JSON fixtures in `web/src/test/fixtures/judge/` (incl. a fixture with forbidden keys that must render nothing from them), Problems markers/filters (no pattern filter)/v1 fallback, Arena (timer off, drawer, Diff, spoiler copy, Mark studied, **no lock frame even with a due touch**), badges (each status → its AB11 badge; 503 + open judged attempt stays judged until practice says `self_grade_pending`).

   Then write `internal/e2e/judge_dock_test.go`, tagged **`-tags e2e,runner`** (m3-06's real-runner lane). It runs on compose with the privileged `runner` profile (local only), gateway, practice and judge with the dev fixture-content overlay, and it goes into m3-06's Linux **`judge-runner-e2e`** CI job (extend the job to start those services if needed; the plain `e2e` job has no runner). On macOS use the spike's multipass VM or rely on CI. Reach **every** AB08 state through the real BFF per the plan's table:
   - learner solutions for accepted / WA / TLE / CE own code / CE hidden API / RE / MLE in Go, C++ and Python go in `internal/judge/testdata/solutions/<item>/`. Add the missing ones there; the pack's `submissions/wrong/*` are packlint inputs;
   - stop the runner service for `inconclusive`;
   - `contract_changed`: start an attempt, then restart **judge only** with a content-overlay variant whose contract differs, plus a pack variant whose `accepts_contract_hashes` contains the variant's live hash. A pack variant alone gives `spec_mismatch`/503;
   - a new synthetic `fx-NNN` item with a `key` part for `not_evaluated_here`. Don't edit existing `fx-00x` items, whose `contract_hash` feeds the m3-06/m3-09/m3-11 goldens;
   - a > 64 KiB paste; a lowered or seeded L9 quota; `too_fast` / `too_many_pending`; seeded `job_telemetry` for L12 `budget_exhausted`; `judge admin breaker set --level 1|2 --for 1h` for the breaker 429s (and `clear` after); `JUDGE_ADMISSION=closed` for the 503 / `state=off`.

   Every fixture change goes into `internal/judge/testdata/{content,packsrc}`, and then `internal/packspec/fixture_test.go`'s `-update` (`go test ./internal/packspec/ -update`) regenerates `pack/` byte for byte. Never hand-edit `pack/`. Save each response as the vitest golden, and name any state left vitest-only (e.g. `queue_full` if L10 isn't configurable) in the decisions log.

   Add the **arena-never-counts** test. First snapshot `/api/progress`, the week payload, `/api/revision/due`, practice's `GET /state/{problemId}` and the practice outbox count. Then do an arena pass, a reveal and a Mark studied, and assert:
   - `arena_revealed` flips to true **exactly once**;
   - everything else is byte-equal: grade, ceiling, outcome, progress, week, revision due, and the outbox count (no `xlearn.practice.*` event);
   - a later course pass on the same item carries `arena_prior=revealed` and is **not** capped.

   Never make the test pass by not recording the reveal (D17).
7. **[X] Verify** — `npm --prefix web run typecheck && npm --prefix web run lint && npm --prefix web run test && npm --prefix web run build` (then `git checkout -- web/dist/.gitkeep`); `go test -race ./...` (incl. `internal/packspec` fixture byte-equality); `go test -tags e2e,runner ./internal/e2e/...` against compose with the `runner` profile (Linux / multipass VM, or CI's `judge-runner-e2e`). Check the build report: the merge view is in the lazy chunk.
8. **[X] Visual check + docs** — temp `web/vite.mock.config.ts` (never committed) to compare Problems, each dock state and the Arena with AB08–AB11 at 1440 px and 390 px; screenshots into the PR. Update `docs/architecture/api.md` (SPA routes → BFF routes; SPA routes are documented there only). **Verify** that m3-09's arena BFF routes are in m1-06's `apiRoutes()` route-enumeration table with policy `applied`. That test walks BFF routes, not SPA routes; add nothing to it (no BFF change here).
9. **[X] PR** → conventional commit(s) `feat(web): results dock, problems markers, arena, judge badges (AB08–AB11)` with the attribution lines → CI green → squash-merge. **No tag.**

## Constraints

- **Web code only** (plus `internal/e2e` tests, synthetic fixture data under `internal/judge/testdata/`, and wiring the e2e into CI's `judge-runner-e2e` job). No BFF, judge or practice change. If a board needs a field the DTO doesn't carry, stop and report it against m3-09/m3-14; never widen the DTO or infer withheld data client-side (ADR-0029 §2, INV-7).
- **D17 wins over t4:** no arena locks, no reveal cap. **D10:** arena activity never counts toward course progress, grades or revision.
- **Presence by config (ADR-0034 §2):** a 404 from a judge route means "absent" — render v1 with no badges; never an error state.
- **`theme.css` verbatim** (no Tailwind); dark theme; difficulty tokens Easy=`--ds-ok`, Medium=`--ds-warn`, Hard=`--ds-err`. Boards are preview-only references — never import from `design-system/screens/v2/`.
- **Bundle:** the editor and `@codemirror/merge` stay lazy (ADR-0034 §2 lazy chunks); the `vite:preloadError` reload from m3-11 covers them.
- **Content secrecy:** fixtures are synthetic and public; never copy from `../xlearn-evalpack` (m3-01's AGENT.md rule).
- **No migrations here** (so no goose/sqlc change; `sqlc diff` must still pass untouched). No outbox/inbox change.
- **GitOps / D34:** nothing to deploy; no alerting, opscheck, healthchecks.io or Flux Alert anywhere — badges are the product-side signal.
- **Memory-sum rule / ACLs / consumers-before-producers:** not touched (no pod, no stream, no subject).
- **Parallel sessions:** check peers' PRs and worktrees before rebasing onto a moved `main`; don't touch another session's worktree.

## Deliverables

- `web/src/components/judge/ResultsDock.tsx`, `JudgeBadges.tsx` (+ tests); `web/src/screens/Arena.tsx`; updated `Problems.tsx`, `Workspace.tsx` (dock + HUD badge mount), `router.tsx`, `lib/judge.ts`.
- `web/src/test/fixtures/judge/*.json` (recorded from the e2e); `internal/e2e/judge_dock_test.go` (`-tags e2e,runner`: dock states + arena-never-counts) wired into CI's `judge-runner-e2e`; synthetic solutions under `internal/judge/testdata/solutions/<item>/`; fixture additions (a new `fx-NNN` item with a `key` part, the `contract_changed` content/pack variant) under `internal/judge/testdata/{content,packsrc}` with `pack/` regenerated (synthetic only).
- Updated `docs/architecture/api.md` (SPA routes; no change to the BFF route-enumeration table).

## Update status

- [`../sprints/sprint-m3-12.md`](../sprints/sprint-m3-12.md): each task 🔄 → ✅; _Overall_ ✅.
- [`../status.md`](../status.md): Sprint board row (m3-12 ✅); artboard rows AB08–AB11 → "implemented (m3-12, PR #)" (mark them "frozen (PR #, date)" first if m3-11 didn't); M3 stays 🔄; no tag/floor/flag change.
- Decisions log: the arena route shape, `@codemirror/merge` for Diff, study-mode rule (no evaluator for the account → study mode), any DTO gap reported, the AB09 F7 / AB10 F9 judge-off departure (v1 fallback; flagged to the owner), any AB08 state left vitest-only. No ADR expected (check peers' ADR numbers first if one becomes necessary).

## Done when (acceptance)

- [ ] Every AB08 state is reachable in compose (`-tags e2e,runner`, `judge-runner-e2e` green) and renders AB08's copy.
- [ ] The dock shows only allowlisted DTO fields.
- [ ] Problems shows course + arena markers with AB09 F2's filters (no pattern filter), and falls back to v1 when judge is absent.
- [ ] The arena has no lock state; timer off by default; History with Copy to editor + Diff; spoiler confirm records without a cap; Mark studied sets `source=manual`.
- [ ] **Arena never alters course progress** (reveal recorded once; everything else byte-equal; later course pass `arena_prior=revealed`, uncapped).
- [ ] Badges follow `GET /api/judge/status` per AB11 (busy/breaker → "Grader busy", `off` → "Auto-grading off"); none on 404; no open judged attempt ever switches to the uncapped picker.
- [ ] Screens match AB08–AB11 at 1440/390 px; web checks and CI green.

Ship per AGENT.md land-and-sync with **this sprint's release action: merge only** (ships dark in `v1.14.0`, tagged by [m3-13](../sprints/sprint-m3-13.md)) — squash-merge once CI is green, no tag, then `git checkout main && git pull`.
