# sqlc config (ADR-0005): type-safe Go from hand-written SQL, pgx/v5 driver, no ORM.
# Generated output is committed; CI runs `sqlc diff` to keep it in sync. One block
# per service schema as services adopt the platform.
version: "2"
sql:
  - engine: "postgresql"
    # schema.sql declares the (infra-provisioned) identity schema for analysis;
    # the migrations dir supplies the tables. goose only ever reads the latter.
    schema:
      - "internal/identity/store/schema.sql"
      - "internal/identity/store/migrations"
    queries: "internal/identity/store/queries"
    gen:
      go:
        package: "gen"
        out: "internal/identity/store/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
        emit_empty_slices: true
  - engine: "postgresql"
    # curriculum: the read-heavy content rail (S03). Same pattern as identity —
    # schema.sql declares the infra-provisioned schema, migrations supply tables.
    schema:
      - "internal/curriculum/store/schema.sql"
      - "internal/curriculum/store/migrations"
    queries: "internal/curriculum/store/queries"
    gen:
      go:
        package: "gen"
        out: "internal/curriculum/store/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
        emit_empty_slices: true
  - engine: "postgresql"
    # practice: the per-user problem lifecycle + transactional outbox (S05). Same
    # pattern — schema.sql declares the infra-provisioned schema, migrations supply
    # tables.
    schema:
      - "internal/practice/store/schema.sql"
      - "internal/practice/store/migrations"
    queries: "internal/practice/store/queries"
    gen:
      go:
        package: "gen"
        out: "internal/practice/store/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
        emit_empty_slices: true
  - engine: "postgresql"
    # review: the five-touch spaced-repetition scheduler + transactional outbox +
    # idempotent inbox (S06). Same pattern — schema.sql declares the infra-provisioned
    # schema, migrations supply tables.
    schema:
      - "internal/review/store/schema.sql"
      - "internal/review/store/migrations"
    queries: "internal/review/store/queries"
    gen:
      go:
        package: "gen"
        out: "internal/review/store/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: false
        emit_empty_slices: true
